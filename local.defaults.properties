# Committed fallback so the project configures even without a secrets.properties.
# The placeholder is not a usable key — the map will load but show no tiles until
# you put a real key in secrets.properties (see README).
MAPS_API_KEY=YOUR_API_KEY_HERE
